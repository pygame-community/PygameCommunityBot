services:
  pygamecommunitybot-dev:
    image: pcbot-dev:latest
    restart: unless-stopped
    build:
      context: .
      dockerfile: Dockerfile
    tty: true
    # Add helpful development volumes here
    # volumes:
      - ...:...
